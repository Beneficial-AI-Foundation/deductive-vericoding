# Synthesizing reader programs

These programs read the state of the environment.

See `DeductiveVericoding/Reader.lean`. The example is still incomplete, because I need a way to write to the environment as well, so using a State monad rather than a Reader monad.